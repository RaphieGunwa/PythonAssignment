# Assuming the list is stored in a variable named 'arr'
arr = [4, 7, 1, 5, 11, 53, 12, 46, 84, 23]

# Calculating the average value of the list
average = sum(arr) / len(arr)
print("The average value of the list is:", average)
